node_modules/
elm-stuff/

# config.js is no longer part of the project — the App ID is stored via
# the Options page in chrome.storage.sync.  This entry is kept to avoid
# accidentally committing any leftover local copy.
config.js
